{
  "buildCommand": "prisma db push --accept-data-loss && npm run db:seed && next build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/update-live-results",
      "schedule": "*/10 * * * *"
    }
  ]
}
